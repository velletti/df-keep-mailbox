{
    "name": "jvelletti/mailsend",
    "description": "Sent auto mails via cron job to keep a rare used mailbox alive. DomainFactory / goDaddy will delete (unused) mailboxes.",
    "type": "project",
    "autoload": {
        "psr-4": {
            "JVelletti\\Mailsend\\": "src/"
        }
    },
    "require": {
        "phpmailer/phpmailer": "^6.9",
        "ext-openssl": "*"
    }
}
